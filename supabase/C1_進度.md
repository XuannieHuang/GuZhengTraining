# C1 OAuth 設定 — 進度（2026-06-26 暫停，下週續）

## ✅ 今天已完成
1. **Supabase SQL**：`C1_google_allowlist.sql` 已跑（白名單 + RLS 改白名單檢查）。
2. **Google Cloud 專案**：已建立 `GuZhengTraining`（專案 ID：`guzhengtraining`）。
3. **OAuth 同意畫面**：已設定完成（應用程式名稱 GuZhengTraining、支援/聯絡信箱 xuan7867@gmail.com、使用者類型 **外部/測試模式**）。
4. **OAuth 用戶端（網頁應用程式）**：已建立。
   - **Client ID**：`624772347963-6omq2daj5cqj4apocqfuhri5qfkvsg92.apps.googleusercontent.com`
   - **重新導向 URI**：`https://toxfwcuhugrqwibghiig.supabase.co/auth/v1/callback` ✓
   - **Client Secret**：在 GCP 用戶端詳情頁的「用戶端密鑰／用戶端密碼」（結尾 `…eKxj`），有複製鈕。**這要你自己複製**。
5. **Supabase → Auth → Providers → Google**：開關已開、Client ID 已填正確、Callback 相符。
   - ⚠️ 注意：當時 Client ID/Secret 欄被瀏覽器**自動填入亂值**（email、某存的密碼），我已清掉並改正。

## ✅ 第二次（2026-07-xx）又完成
6. **Supabase URL Configuration**：Site URL = `https://stupendous-biscochitos-26954f.netlify.app`、Redirect URL = `…netlify.app/**`，皆已存檔。
7. **GCP 測試使用者**：已加入 `xuan7867@gmail.com`（測試模式下要在名單內才能 Google 登入）。

## ✅ 第三次完成 —— OAuth 連線已驗證成功！
8. **Supabase Google provider**：已 Enable、Client ID 正確、Client Secret 已貼（用 GCP「Add secret」新增的那組）、已存檔。
9. **端到端測試通過**：直接打 `…/auth/v1/authorize?provider=google` → Google 選帳戶 → 完成登入 → 成功進入 App。代表 Client ID／Redirect URI／**Secret**／白名單全部正確。
   - 註：瀏覽器 autofill 會狂塞假值（email 當 Client ID、`221014` 當 Secret），重設時務必確認正確再存。

## ✅ 第四次完成 —— 部署上線（GitHub 自動部署）
10. **GitHub repo**：`XuannieHuang/GuZhengTraining`（Private），整個 `D:\app` 已推上（`.gitignore` 排除 secret JSON、xlsx、.claude）。
11. **Netlify 接 GitHub 自動部署**：現有站台 `stupendous-biscochitos-26954f` 已連 repo，**Publish directory = `prototype`**、無 build command。網址不變，OAuth 設定全部沿用。
12. **新版已上線並驗證**：登入頁有「用 Google 登入」、新功能（匯出、季繳>10 等）都生效；用 Google 登入成功進入 App。
13. **以後改版**：`git push` → Netlify 自動部署，免手動。

## ⛔ 還沒做（剩這些小事）
1. **其他 2 位管理者的 Gmail**：給我後，我幫你加進 ① GCP 測試使用者 ② `allowed_emails` 白名單（或你在設定頁加）。
   - 註：App 目前是「測試模式」，只有 GCP 測試使用者清單內的 Gmail 能完成 Google 登入。等之後想開放，可在 GCP 目標對象按「發布應用程式」。
2. **跑 `B_teacher_changes.sql`**（老師異動歷史的表，還沒跑；沒跑的話換老師會跳提示）。

## 備註
- Client Secret 屬機密，未寫入任何檔案；放在 GCP 用戶端頁，由你親自複製貼上。
- 我（Claude）依規定不代填機密金鑰，所以「貼 Secret」那步一定是你操作。
