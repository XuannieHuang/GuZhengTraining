# 教室管理 App — 功能說明書（目前實作版）

> 古箏／琵琶教室的**管理端** App（不做學生端）。手機快速核對紙本、追蹤繳費與器材租借。
> 本檔記錄「目前已經做出來的功能與架構」。未來規劃見 `未來計畫.md`。早期設計草案見 `規格文件.md`（部分已過時，以本檔為準）。
> 最後更新：2026-06（隨開發持續更新）

---

## 1. 技術架構

- **前端**：單一 PWA，已拆成模組化檔案（不是一個大檔）。純前端、無編譯步驟（這台電腦沒裝 Node，刻意不用 Next.js）。
- **後端／資料庫／登入**：Supabase（PostgreSQL + Auth + Row Level Security）。專案 ref `toxfwcuhugrqwibghiig`。
- **部署**：Netlify（拖資料夾上傳）。線上版 `https://stupendous-biscochitos-26954f.netlify.app`。
- **連結整理**：見 `重要連結.md`。

### 檔案結構（`D:\app\prototype\`）
```
index.html         ← 外殼，依序引用下列 CSS / JS
manifest.json, icon-192/512/180.png   ← PWA 圖示與設定
_headers           ← Netlify：不快取，重新部署即更新
css/
  base.css         ← 共用：版面、卡片、徽章、表格(.dtable)、彈窗(.sheet)、按鈕、toast
  login.css todo.css check.css rental.css report.css settings.css  ← 各頁專屬
js/
  config.js        ← Supabase 金鑰、建立 client（變數 sb）
  data.js          ← 全域狀態(DB/ui/PLANS/REPORTS/ME)、欄位對應(mapXxx)、共用邏輯、loadAll、toast
  view-login/todo/check/rental/report/settings.js   ← 各頁渲染
  modal-student/payment/rental/teacher/plan.js      ← 各彈窗
  app.js           ← render()外殼、底部導覽、全域匯出(window.*)、boot()啟動（最後載入）
```

### 程式運作原則
- 傳統 `<script>` 依序載入，所有函式為全域；inline `onclick` 直接呼叫。
- 狀態：`DB`（teachers/students/rentals 快取陣列）、`ui`（畫面狀態）、`PLANS`、`REPORTS`、`ME`/`MY_EMAIL`。
- 每個動作各自寫回 Supabase（樂觀更新：先改畫面、再非同步存）。
- **localhost 預覽會快取舊 JS；驗證時不要 eval `data.js`（內有 `let DB` 會被遮蔽），改用重整頁面。**

---

## 2. 資料表（Supabase）

| 表 | 重點欄位 |
|----|----------|
| **teachers** | name, role(主任/老師), instruments(text[]), active, sort |
| **students** | name, teacher_id, instrument, class_type, plan(月繳/季繳), attended(已上堂數), last_pay_date, last_pay_amount, status(在學/新生/停課/待確認), note, checked, notified, contact(未用), prepay_amount(未用), partner_id(雙人組配對，雙向) |
| **plans** | instrument, class_type, billing(月繳/季繳), sessions, price（unique 樂器×班型×繳別） |
| **payments** | student_id, billing, periods, amount, pay_date（每筆繳費 log） |
| **rentals** | student_id, renter_name, instrument, billing, pay_date, start_date, due_date, status(租賃中/已還箏), returned_date(還箏日), note |
| **rental_logs** | rental_id, billing, pay_date, start_date, due_date（每期租借歷史） |
| **teacher_changes** | student_id, from_teacher_id, to_teacher_id, changed_at（學生換老師歷史） |

- 樂器／班型的 CHECK 限制**已放寬**（可自由新增新樂器/班型）。
- RLS：所有表僅 `authenticated` 可存取（登入後才看得到，外人只見登入頁）。
- SQL 檔在 `D:\app\supabase\`：schema.sql、relax_constraints.sql、rentals_active.sql、seed_students.sql 等。

---

## 3. 核心邏輯（最重要）

### 堂數與繳費（記繳費自動扣堂）
- **已上堂數**＝你手動維護的數字（可負）；核對頁直接 inline 改。
- **記繳費**：確認繳費時，系統**自動把已上堂數 −（方案堂數 × 期數）**（季 12／月 4／單 1），等於滾到下一期。例：已上 11、繳季繳 1 期 → 11−12 = −1。
  - ⚠️ 改這版後**不要再手動扣堂**，否則會扣兩次。
- **記繳費入口**：① 待辦頁點學生即跳記繳費彈窗 ② 編輯學生 modal 內。核對頁已移除 `$`。
- **事件修正**（改/刪/退費舊繳費）**不會**自動動堂數，純修正紀錄用；要調堂數直接改數字。

### 提醒門檻（待繳費判斷）
- 月繳：已上堂數 **≥ 3**（剩 ≤1）→ 列入待辦
- 季繳：已上堂數 **> 10**（即 ≥11，剩 ≤1）→ 列入待辦
- 只看你打的已上堂數；記繳費後自動扣堂、變成負數 → 自動移出待繳。

### 器材到期判斷（rentalState）
- 到期日 < 今天 → **逾期**；到期日在本月 → **本月到期**；其餘 → 租賃中；狀態已還箏 → returned。

---

## 4. 各頁功能（底部 5 分頁：待辦／核對／租借／報表／設定）

### 登入
- Supabase Email/密碼登入；不開放自由註冊（帳號由管理者在後台建立）；外人只見登入頁。

### 待辦（todo）
- 標題列顯示「本期即將完課 N 位」（紅點）。
- **表格**：姓名／方案／已上堂數／備註，**依老師分組**（老師名＋職稱、N 位），方案靠左對齊「月/季」對齊分組標題的數字，備註過長換行不截斷。
- 只列達門檻（待繳費）的在學/新生學生。
- **點學生列即開記繳費彈窗**（待繳名單直接記；記後已上堂數自動扣方案堂數）。
- 頂部 **⬇ 匯出待繳名單（CSV）**：老師/姓名/方案/已上堂數/備註，含老師停用的「未指派」待繳生，當催繳清單用。

### 核對（check）— 每天主要操作頁
- 上方**老師頁籤**切換（只顯示在職老師）；多一個 **「⚠ 待指派」分頁**（老師停用後其在學學生變孤兒，列在此供改派，處理完消失）。
- 學生列：**inline 直接改已上堂數**（不跳視窗）、改過自動打勾＋變綠底、未上課可點 ✓ 標示已核對；達門檻數字變紅。
- **已核對自動下移**：打勾／改過堂數的學生自動移到下方「已核對 N」群組，上方待核對清單越來越短，不必上下滑找未核對的人。
- 第二行顯示「上次繳費 M/D · 備註」。
- 只顯示在學/新生（停課/待確認不顯示）。
- 點**姓名**開編輯學生（記繳費已移到待辦頁與編輯學生 modal，核對頁不再有 `$`）。雙人班學生會顯示 **👥 同學名／未配對** 綠色徽章。
- 有勾選時，頁頂出現 **「清除全部 · 開始新一梯」**：一鍵清掉**所有老師**的核對勾選（只清勾選、不動已上堂數），方便每週／隔週開新一輪核對。
- **＋新增學生**（預設帶該老師）。

### 記繳費（待辦頁點學生開的彈窗）
- 方案膠囊（依學生樂器，琵琶只月繳）＋ 期數(1/2/3) ＋ 金額（自動帶上次金額或方案常用價、可改）＋ 繳費日（預設今天）＋ 合計。
- **確認後：記一筆 payments log ＋ 已上堂數自動 −（方案堂數 × 期數）**（季 12／月 4／單 1），等於滾到下一期。

### 學生管理 modal（新增/編輯）
- 姓名、任課老師(膠囊)、**學習樂器**(動態取自方案)、**授課類型**(動態)、方案、狀態(在學/新生/停課/待確認，無退學)、已上堂數、**備註(多行 textarea)**。
- **內嵌「💲 記一筆繳費」區塊**（不另開彈窗）：方案/期數/金額/繳費日/合計＋確認鈕；確認後同樣自動扣堂並更新繳費歷史與已上堂數。
- **授課類型選「雙人」時**多一欄「雙人組同學」：可挑同老師·雙人班·在學的另一位學生，存檔時**雙向自動同步**（改配對會自動拆掉舊的一端）；第一人建檔、還沒有對象時顯示「等對方建檔後再配對」。各自繳費互不影響。
- 編輯時下方顯示**繳費歷史**（每筆日期/金額/方案，可點進編輯）。
- 換老師時自動記**老師異動歷史**（`teacher_changes`：何時·從誰→到誰）；有紀錄才顯示該區塊。

### 老師管理（在設定頁）
- **新增/編輯**（姓名、角色主任/老師、教授樂器可多選）。
- **啟用/停用**：停用的預設收進「顯示已停用」折疊；停用時若名下有在學學生會跳提示列出姓名。
- 不做刪除（只停用，較安全）。

### 方案管理（在設定頁）
- **新增/編輯/刪除**方案；樂器、班型可**自由打字**（開新課用）；新增後學生表單的樂器/班型選項自動跟著出現。

### 器材租借（rental）
- 分頁：**租賃中／已還箏**；＋新增租借。標題列（同待辦樣式）顯示 **逾期 N・本月到期 N** 統計。
- 清單**依器材分組**（古箏/琵琶），移除器材欄；欄位 姓名／起租日／到期日／狀態。
- 字級對齊待辦（姓名15、日期17、狀態14）；姓名下方顯示**備註**。
- 狀態用**彩色標籤**（逾期紅／本月到期琥珀／租賃中灰，無 emoji）；到期日逾期紅、本月到期琥珀。
- **彈窗**：承租人、樂器、方案(月/季)、繳費日（≠起租日）、起租日→自動帶到期日（**到期日可手動改**）、備註、狀態、**還箏日**(切已還箏時出現、預設今天)、**租借歷史**(繳費日在前、新到舊)、續租一期/儲存/取消/刪除。
  - **編輯儲存時同步更新對應的租借歷史那一期**（繳費日/租期改了，歷史也跟著改，不再不一致）。
- **續租一期**：可**選續租繳費日**（預設今天）；起租日接續上一期到期日、到期日再 +一期，並寫一筆 rental_logs。

### 報表（report）
- **現況總覽**：上課中學生(含新生)、本期待繳、器材租賃中、器材逾期+本月到期。
- **各老師在學學生數**。
- **月營收**（可切月份）：總額、依繳別、依老師、本月繳費明細（來源 payments，只含 App 內記的繳費，歷史匯入未建 payment）。明細**每列可點**→開繳費事件編輯。
- **已停課（流失）名單**：每列有 **↩ 復課** 按鈕，可把誤設停課的學生改回「在學」。
- 頂部 **✎ 事件修正** 按鈕 → 進事件修正頁。
- 底部 **📥 資料匯出（CSV）**：可匯出**繳費明細**（日期/學生/老師/方案/期數/金額/合計）與**租借清單**（承租人/樂器/方案/各日期/狀態/備註）；含 UTF-8 BOM，Excel 開中文不亂碼。

### 事件修正（events，從報表頁進入）
- 繳費＋租箏整合清單，可篩選（全部／繳費／租箏）、搜尋姓名、新到舊排序。
- 點**繳費**事件 → 編輯 modal：改 方案/期數/金額/日期、**刪除**、**退費（另記負數）**；不影響已上堂數。
- 點**租箏**事件 → 既有租借 modal（改租期／續租／還箏／刪除）。
- 繳費編輯入口共三處：此頁、報表明細列、學生 modal 的繳費歷史列。

### 設定（settings）
- 方案常用價（全部可改/新增）、老師管理、登入帳號（顯示目前登入者，帳號於 Supabase 建立）、登出。

---

## 5. 已知行為/注意
- 營收報表只反映「App 內用 💲 記的繳費」；Excel 匯入的歷史只帶了最後繳費日，沒有逐筆 payment。
- 彈窗開啟會鎖背景捲動（避免捲到背景頁）。
- 部署更新：把 `D:\app\prototype` 整個資料夾拖到 Netlify Deploys。
