# 教室管理 App — 未來計畫（plan）

> 尚未實作、未來想做的項目。已完成的功能見 `功能說明.md`。
> 最後更新：2026-06

---

## A. 新規劃（2026-06 提出，優先處理）

### A1. 群組機器人（提示繳費／租箏）
- 加一個機器人到群組（推測 LINE 群組），讓機器人**主動提示**：
  - 哪些學生待繳費（本期即將完課）。
  - 哪些器材租借即將到期／逾期。
- 需評估：用哪個平台（LINE Messaging API / LINE Notify）、由誰觸發（排程）、訊息內容格式。
- 與「微型後端」相關（機器人推播通常需要一個後端排程，見 C）。

### A2. 獨立事件編輯頁 — **已完成（2026-06）**
- **繳費事件編輯 modal**：可改 方案/期數/金額/日期、**刪除整筆**、**退費（另記一筆負數，原紀錄保留）**。不影響已上堂數（維持脫鉤）。
- **事件修正分頁**（報表頁「✎ 事件修正」進入）：繳費＋租箏整合清單，可篩選（全部/繳費/租箏）、搜尋姓名、newest first；點繳費→編輯 modal，點租箏→既有租借 modal。
- **入口（兩者都要）**：① 報表「本月繳費明細」每列可點 ② 學生 modal 的繳費歷史每列可點 ③ 事件修正分頁查找。
- 不留獨立異動 log（退費以負數紀錄表達，報表營收自動扣除）。

### A3. Google 登入 + 白名單（強化安全與帳號管理）
- **目標**：改用 Google 一鍵登入；只有**email 已加入白名單**的人才能使用（你先在 DB 設定，對方才能用）。比現在「手動建帳號 + 設密碼」更安全、更好管理（你只維護一份 email 清單，不碰密碼）。
- **觀念**：登入(authentication) 仍用 Supabase 內建（Google OAuth）；白名單只是多加一道「授權(authorization)」的門。不是自己造登入系統。
- **為什麼需要白名單**：Google 任何人都能登入並被自動建身分，所以「能登入」不再是門 → 用白名單 + RLS 當真正的門。沒在名單上的人即使 Google 登入成功，RLS 也全擋。
- **實作步驟**：
  1. 建 `allowed_emails` 表（管理者把允許的 email 加進去）。— SQL，我給。
  2. 改 RLS：所有表的政策從「`to authenticated using(true)`」改成「登入者 email 必須在 allowed_emails 裡」。— SQL，我給。
  3. 設定 Google OAuth：Google Cloud Console 建 OAuth 憑證 → client id/secret 填進 Supabase（**最繁瑣、一次性**，我帶圖文）。
  4. App 加「用 Google 登入」按鈕（`sb.auth.signInWithOAuth({provider:'google'})`）+「尚未開通」提示。— 我寫。
  5. 設定頁的「管理者」區塊改成**真的白名單管理**（App 內加/刪 email）——呼應之前擱置的「App 內名冊」。— 我寫。
- **與目前的差別**：現在 Email/密碼是「能登入＝有權限」；改後是「Google 登入 + email 在白名單」兩條件才放行。可只用 Google（推薦）或與 Email/密碼並存。
- **與機器人擔憂的關係**：解決「anon 公開金鑰」的疑慮——金鑰公開沒關係，因為真正的門變成 Google 登入 + 白名單。

---

## B. 既有待辦（之前討論過、尚未做）

### B'. 2026-07 新提出
- **核對頁學生名單旁顯示「是否租箏」**：一眼看到該生有沒有租借器材。
- **學生資料輸入頁一起記錄租箏資訊**：新增/編輯學生時可同時建租借（少一個步驟）。
- **資料結構整理／精簡**：目前功能散落各處，評估是否能統整、精簡（學生／繳費／租借之間的關聯）。

- ~~**雙人組配對**~~ — **已完成（2026-06）**：雙人班可在學生 modal 選「雙人組同學」（同老師·雙人班·在學者），雙向自動同步；核對頁顯示 `👥 同學名／未配對` 徽章；第一人建檔時顯示「等對方建檔後再配對」提示。各自繳費互不影響。
- ~~**老師異動歷史**~~ — **已完成（2026-06）**：學生換老師時自動記一筆到 `teacher_changes`（何時、從誰→到誰）；學生 modal 有異動時顯示「老師異動歷史」區塊（新到舊）。涵蓋核對頁「待指派」改派的情況。需先跑 `supabase/B_teacher_changes.sql`。
- **已還箏清單**：套用一樣的彩色狀態標籤；可選顯示「還箏日」。
- **新生→在學自動轉**：曾討論「新生繳第二期自動轉在學」——目前決定先不做（手動即可）。
- **預收下期金額**：曾討論——決定不做（備註欄已足夠）。

---

## C. 微型後端評估（已評估，結論如下）

- **關鍵釐清**：本專案目前已是「純前端 + Supabase（Postgres/Auth/RLS）」＝公開 anon key + RLS 的架構。資料庫**維持 Supabase，不要換**（換 Mongo/Neon/Sheets/Firebase＝無謂重做）。
- **真正需要後端的只有「A1 機器人推播」**：需定時(cron) + 機密(LINE token 不能放前端)。A2 事件編輯頁不需後端。
- **推薦**：機器人後端用 **Supabase Edge Functions + 排程(Cron)**（最整合、同一平台、token 放 Supabase 環境變數，函式我寫）。最省事替代：**Google Apps Script** 當排程器（讀 Supabase REST + 推 LINE，UI 設定時間觸發、零 CLI）。
- **「上 Git 自動部署」≠ 後端**：只是把「手動拖資料夾」變成「push 自動部署」，架構不變。若改用 Vercel/Cloudflare(綁 Git)，可把機器人函式放同一 repo 一起部署（順便的好處，非必要）。
- **金鑰觀念**：前端的 anon key 是公開設計、藏不住也不需藏；真正的防護是 RLS + 不開放註冊（或 A3 白名單）。LINE token 等真機密才放後端環境變數。
- **LINE 注意**：LINE Notify 已於 2025 停用，群組推播要用 LINE Messaging API（bot 加進群組、取得 group id），實作時再帶。

---

## D. 開發流程 / 程式碼品質

- ~~**上 GitHub 簡化上版流程**~~ — **已完成（2026-06）**：
  - GitHub repo：`XuannieHuang/GuZhengTraining`（Private），整個 `D:\app` 版控（`.gitignore` 排除 secret/xlsx/.claude）。
  - Netlify 現有站台 `stupendous-biscochitos-26954f` 已接 repo 自動部署，**Publish directory = `prototype`**、無 build command、網址不變。
  - 流程：改完 `git push` → Netlify 自動部署。`_headers` 在 `prototype/` 內沿用。
- CSS 樣式整理：移除 JS 模板裡的 inline `style=`，集中到各 CSS 檔 ——**已完成（46→0）**。
