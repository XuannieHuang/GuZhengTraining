# 部署到 Netlify（讓手機隨時能開）

> 你的 App 是純靜態網頁，用 Netlify Drop「拖資料夾」就能上線，約 3 分鐘。

---

## 第一次部署

1. 用瀏覽器打開 **https://app.netlify.com/drop**
2. 開檔案總管，找到資料夾 **`D:\app\prototype`**
3. **把整個 `prototype` 資料夾**拖進網頁中間的虛線框（不是單一檔案，是整個資料夾）
4. 等幾秒，上面會出現一個網址，像 `https://random-name-12345.netlify.app`
5. 點那個網址 → 看到登入頁就成功了 🎉

> 提示：拖「資料夾」，因為裡面有 index.html + css/ + js/ + 圖示 + manifest，要一起上去。

---

## 建議：註冊帳號「認領」這個站（這樣才不會過期、之後能改）

1. 部署完，網頁會提示 **Sign up to claim / 認領這個網站** → 用 Google 註冊登入
2. 認領後，到 **Site settings（網站設定）→ Change site name**，可以改成好記的網址，例如 `your-classroom.netlify.app`

---

## 之後要更新版本（我改了程式之後）

認領網站後：

1. 進到該網站的 **Deploys（部署）** 分頁
2. 把 `D:\app\prototype` 資料夾**再拖一次**到頁面下方的拖放區 → 覆蓋更新
3. 手機重新整理就是新版

（或進階一點可以綁定自動部署，之後需要再說。）

---

## 手機加入主畫面（像 App）

- **iPhone**：用 **Safari** 開網址 → 分享鈕 → 「加入主畫面」→ 出現「教室管理」圖示
- **Android**：用 Chrome 開網址 → 右上選單 → 「加入主畫面 / 安裝應用程式」

加入後打開沒有網址列，跟原生 App 幾乎一樣。

---

## 安全性

- 網址公開沒關係：外人開只看到**登入頁**，資料由登入 + RLS 保護。
- Email／密碼登入跨網域可正常運作，不用額外設定。
- 3 位管理者各自用在 Supabase 建立的帳號登入，共用同一份雲端資料。
